import React, { FC } from "react";
import HomeHeader from "./homeHeader/HomeHeader";

const HomePage: FC = () => {
  return (
    <div>
      <HomeHeader />
    </div>
  );
};

export default HomePage;
